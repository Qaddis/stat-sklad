<h1 align="center">СтатСклад</h1>

## ℹ️ Общее

- [Miro (фичи, схема БД)](https://miro.com/app/board/uXjVJmwfigw=/?share_link_id=495611814831)

### Порядок действий:

1. Авторизация
2. Поставки/списывания
3. Вывод склада
4. История действий
5. Автоматическое списание продуктов
6. Данные о пользователе

### Эндпоинты

#### Авторизация

- `/auth/sign-up` - **Регистрация**: принимает основные данные, если email не встречается в базе - возвращает access-token + refresh-token
- `/auth/sign-in` - **Вход в аккаунт**: если email и пароль (хэш) правильные, то возвращаем access-token + refresh-token
- `/auth/refresh` - **Обновление пары токенов**: обновление пары токенов по refresh-token-у и возвращение пары токенов

#### Пользователь

- `/user` - **Вывод основной информации** (имя и фамилий, email)

#### Склад / Основное

- `/products/get` - **Вывод списка продуктов со склада** (пагинация, поиск)

- `/stat/*` - **Статистика ...**: возвращает названия продуктов и метрики
- `/stat/*` - **Статистика ...**: возвращает названия продуктов и метрики
- `/stat/*` - **Статистика ...**: возвращает названия продуктов и метрики

#### Склад / Действия

- `/actions/new-supply` - **Создание новой поставки**: принимает данные с фронта, создаёт новую поставку со всеми необходимым зависимостями, сохраняет поставку в историю действий и возвращает на фронт ошибку/статус операции
- `/actions/write-off` - **Списывание продуктов со склада**: принимает данные с фронта, создаёт новую операцию списывания со всеми необходимым зависимостями, сохраняет операцию списывания в историю действий и возвращает на фронт ошибку/статус операции
- `/actions/history` - **История операций**: возвращает все операции списывания и поставки, отсортированные по датам (от новым к старым) с пагинацией

#### Прочее

- `/actions/cook` - Списывает часть продуктов со склада. Нужное кол-во продуктов считать автоматически из meals и т.д. и т.п.

## 🛠️ Стек

- Backend:

  - ЯП - **Python 3.13**
  - Фреймворк - **Fastapi**
  - ORM - **SQLAlchemy 2.0**
  - Миграции - **Alembic**
  - Валидация - **pyDantic**

- Frontend:
  - ЯП - **TypeScript**
  - Фреймворк - **Next.js**
  - Стили - **SCSS**
  - Серверный стейт-менеджер - **React Query**
  - Клиентский стейт-менеджер - **Jotai**
  - Запросы - **axios**
  - Валидация данных - **Zod**
  - Работа с формами - **react-hook-form**
